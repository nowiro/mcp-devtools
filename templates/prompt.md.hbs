---
description: '{{description}}'
mode: agent
---

# /{{trigger}}

<!--
AUTO-GENERATED by `npx mcp-devtools-cdk compile`.
Source: src/cdk/workflows/{{kebab id}}.workflow.ts
DO NOT EDIT BY HAND — changes will be overwritten on next compile.
-->

## Cel

{{description}}

## Recipe

> Każdy step ma `Type`, `Bind output to` (zmienna do późniejszego użycia w
> `\{{vars.<name>}}`), i `Args` / `Prompt` / `Question`.
> Wykonuj sekwencyjnie. Po każdym `mcp_call` zweryfikuj output przed następnym
> step'em.

{{#each steps}}

### Step {{n}} — {{title}}

- **Type:** `{{type}}`
{{#if (eq type "mcp_call")}}
- **Tool:** `{{tool}}`
{{#if timeoutMs}}
- **Timeout:** {{timeoutMs}} ms
{{/if}}
- **Args:**

```jsonc
{{{json args}}}
```
{{#if bind}}
- **Bind output to:** `{{bind}}`
{{/if}}
{{/if}}
{{#if (eq type "llm_reason")}}
{{#if inputFrom}}
- **Input from:** `{{inputFrom}}`
{{/if}}
- **Reasoning prompt:**

> {{prompt}}

{{#if outputSchema}}
- **Output schema:**

```jsonc
{{{outputSchema}}}
```
{{/if}}
{{#if bind}}
- **Bind output to:** `{{bind}}`
{{/if}}
{{/if}}
{{#if (eq type "user_input")}}
- **Question:** {{question}}
{{#if default}}
- **Default:** `{{default}}`
{{/if}}
{{#if choices}}
- **Choices:** {{#each choices}}`{{this}}`{{#unless @last}} / {{/unless}}{{/each}}
{{/if}}
- **Bind to:** `{{bind}}`
{{/if}}
{{#if description}}

{{description}}
{{/if}}

{{/each}}

---

## Related

- Source TS: [`src/cdk/workflows/{{kebab id}}.workflow.ts`](../../src/cdk/workflows/{{kebab id}}.workflow.ts)
- Architecture: [`docs/explanation/sdd-architecture.md`](../../docs/explanation/sdd-architecture.md)
